# Local health port: same passthrough as public 443, bound to loopback only.
# Lets the flip script verify nginx->backend passthrough without touching 443.
server {
    listen 127.0.0.1:14443 so_keepalive=5m:75s:6;
    proxy_pass xray443;
    proxy_connect_timeout 5s;
    proxy_timeout 1h;
}
